---
description: RPS报表防回归规则（分组、展示、生成一致性）
alwaysApply: true
---

# RPS Report Regression Guard

适用目标：`rps_report_final_*.py`、`web_app.py`、`run_*.bat` 相关修改。

## 1) 版本迭代约束
- 新需求默认在新版本文件上实现（如 `*_03.py`、`*_04.py`），不要破坏用户确认过的旧版本。
- 未经用户明确同意，不回退到旧实现。

## 2) 行业/概念分组硬规则
- 板块分组必须严格按代码前缀：
  - `880xxx` -> 行业板块
  - `881xxx` -> 概念板块
- 禁止使用 `type` 字段回退判断行业/概念，避免串组或反组。
- 行业视图只能显示行业数据；概念视图只能显示概念数据。

## 3) 页面结构与信息完整性
- 不得移除以下核心信息，除非用户明确要求：
  - 核心池/趋势池/新生池
  - 当前池子板块列表
  - 相对前一日新增/退出信息
- 如存在“当日前20”需求，必须提供独立视图，不受分池阈值限制。

## 4) 图表生成一致性
- 前后端必须使用同一套 chart 文件名规则（统一函数）。
- 双击板块打开图表必须命中已生成文件。
- 图表生成范围默认覆盖“页面可见板块”，不能只生成最新池成员导致缺图。

## 5) Web 触发与缓存
- 页面加载后应可自动触发一次报告生成（异步）。
- 缓存 key 必须包含：
  - extdata 版本（mtime）
  - 报告代码版本（目标报告模块 mtime）
  - 报告模块名
- 代码改动后必须可自动刷新报告，不可继续复用旧 HTML。

## 6) 交付前自检（必须执行）
每次改动后，至少验证：
- 行业/概念切换是否正确（无串组）
- 核心/趋势/新生/前20四个页签是否存在且可渲染
- 当前池子列表与新增/退出是否展示
- 点击板块名高亮 + 右侧预览/双击新窗口图表是否可用
- 关键接口（`/api/report/status`、`/api/report/trigger`）返回正常
